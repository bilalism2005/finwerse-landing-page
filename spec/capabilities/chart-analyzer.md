# Capability: Chart Analyzer

## Status: Not built — placeholder only

Not yet spec'd. No router, service, or data model exists for this capability anywhere in `apps/api` as of this migration (2026-08-23). The original `PRODUCT_CONTEXT.md` describes it only as: "pattern detection, plain-language explanation, reliability score" — to be spec'd in a future session.

## What It Does (intended, not implemented)
Chart pattern detection with a plain-language explanation and a reliability score for the detected pattern.

## Next Step
Run `/zero-shot-build` (once the ported skill exists — see `CLAUDE.md`) on this capability specifically to drive a full spec-writer pass — inputs, outputs, external calls, business rules, and success criteria — before any implementation work starts. Do not begin coding against this placeholder file.
